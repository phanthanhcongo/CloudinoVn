---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Resources | Cloudino">
  <main class="bg-black">
    {/* Hero Section */}
    <section
      class="relative flex min-h-[550px] sm:min-h-[750px] md:h-[930px] items-center justify-center overflow-hidden bg-[#080511]"
    >
      {/* Background Vertical Stripes */}
      <div
        class="absolute inset-0 bg-[linear-gradient(to_right,rgba(255,255,255,0.04)_1px,transparent_1px)] bg-[size:40px_100%] sm:bg-[size:60px_100%] pointer-events-none opacity-50 z-0"
      >
      </div>

      {/* Background Starry Noise */}
      <img
        src="/images/backgrounds/image119.png"
        alt=""
        class="absolute inset-0 h-full w-full object-cover mix-blend-lighten opacity-60 pointer-events-none brightness-150"
      />

      {/* Glowing Purple Ambient Orb in Center */}
      <div
        class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 h-[200px] w-[200px] sm:h-[320px] sm:w-[320px] md:h-[450px] md:w-[450px] rounded-full bg-gradient-to-tr from-[#312E81] via-[#6D28D9] to-[#EC4899] opacity-40 blur-[80px] pointer-events-none z-0"
      >
      </div>

      {/* Centered Orbit Ring Container */}
      <div
        class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-[320px] h-[320px] sm:w-[520px] sm:h-[520px] md:w-[680px] md:h-[680px] lg:w-[780px] lg:h-[780px] aspect-square pointer-events-none z-10 flex items-center justify-center"
      >
        {/* Dashed Orbit Ring */}
        <div
          class="absolute left-1/2 top-1/2 h-full w-full rounded-full border border-dashed border-purple-300/40 animate-spin-slow"
        >
          {/* Connector Dots on Orbit Ring */}
          <div
            class="absolute top-[10%] left-[22%] -translate-x-1/2 -translate-y-1/2 h-4 w-4 rounded-full bg-[#fee2e2] border-2 border-white shadow-[0_0_14px_#ffffff]"
          >
          </div>
          <div
            class="absolute top-[50%] right-[0%] translate-x-1/2 -translate-y-1/2 h-4 w-4 rounded-full bg-[#fee2e2] border-2 border-white shadow-[0_0_14px_#ffffff]"
          >
          </div>
          <div
            class="absolute bottom-[8%] left-[38%] translate-x-1/2 translate-y-1/2 h-3.5 w-3.5 rounded-full border border-white/70 bg-white/20 backdrop-blur-sm"
          >
          </div>
        </div>

        {/* 3D Floating Objects Anchored Directly On Orbit Ring Curve */}
        {/* 1. Top-Left Hexagon Nut */}
        <div
          class="absolute top-[30%] left-[4%] -translate-x-1/4 -translate-y-1/4 animate-float"
          style={{ animationDelay: "1s" }}
        >
          <img
            src="/images/resources/nut-3d.png"
            alt="3D Ring Nut"
            class="h-[85px] w-[85px] sm:h-[145px] sm:w-[145px] md:h-[190px] md:w-[190px] object-contain rotate-[10deg] filter drop-shadow-[0_15px_30px_rgba(125,81,211,0.6)]"
          />
        </div>

        {/* 2. Top-Right Cone */}
        <div
          class="absolute top-[30%] right-[4%] translate-x-1/4 -translate-y-1/4 animate-float"
          style={{ animationDelay: "0s" }}
        >
          <img
            src="/images/resources/cone-3d.png"
            alt="3D Cone"
            class="h-[85px] w-[85px] sm:h-[150px] sm:w-[150px] md:h-[200px] md:w-[200px] object-contain filter drop-shadow-[0_15px_30px_rgba(125,81,211,0.6)]"
            style={{ transform: "rotate(120deg)" }}
          />
        </div>

        {/* 3. Bottom-Left Liquid Blob */}
        <div
          class="absolute bottom-[4%] left-[4%] -translate-x-1/4 translate-y-1/4 animate-float"
          style={{ animationDelay: "1.5s" }}
        >
          <img
            src="/images/resources/blob-3d.png"
            alt="3D Liquid Blob"
            class="h-[90px] w-[90px] sm:h-[155px] sm:w-[155px] md:h-[210px] md:w-[210px] object-contain -rotate-6 filter drop-shadow-[0_15px_30px_rgba(125,81,211,0.6)]"
          />
        </div>

        {/* 4. Bottom-Right 4-Point Star */}
        <div
          class="absolute bottom-[4%] right-[4%] translate-x-1/4 translate-y-1/4 animate-float"
          style={{ animationDelay: "2s" }}
        >
          <img
            src="/images/resources/star-3d.png"
            alt="3D Star"
            class="h-[80px] w-[80px] sm:h-[135px] sm:w-[135px] md:h-[185px] md:w-[185px] object-contain rotate-12 filter drop-shadow-[0_15px_30px_rgba(125,81,211,0.6)]"
          />
        </div>
      </div>

      {/* Center Main Title & Coming Soon Note */}
      <div
        class="relative z-10 mx-auto w-full max-w-[1920px] px-4 sm:px-8 md:px-12 lg:px-16 2xl:px-[285px] text-center"
      >
        <h1
          class="text-5xl sm:text-7xl md:text-[88px] lg:text-[104px] font-extrabold tracking-tight text-transparent bg-clip-text select-none leading-none"
          style="background-image: linear-gradient(135deg, #c7d2fe 0%, #e9d5ff 28%, #fef08a 55%, #fbcfe8 78%, #f472b6 100%); filter: drop-shadow(0 0 30px rgba(168,85,247,0.35));"
        >
          Resources
        </h1>
        <p class="mt-4 text-xl sm:text-2xl md:text-3xl font-medium tracking-widest text-purple-200/80 uppercase">
          Coming Soon
        </p>
      </div>
    </section>

    <!-- <section class="bg-black pb-8">
      <div class="mx-auto max-w-[1920px] px-[285px]">
        <div class="flex gap-4">
          {
            ["All", "Blog", "New", "Event"].map(tag => (
              <button class="flex flex-col items-start rounded-full border border-white/20 px-6 py-3 text-white/70 transition-all hover:border-white/40 hover:text-white">
                <span class="text-2xl font-normal">{tag}</span>
                <span class="text-base font-normal opacity-50">
                  Optimize cloud spending
                </span>
              </button>
            ))
          }
        </div>
      </div>
    </section>

    {
      ["AWS", "Digital Ocean", "Lark"].map(category => (
        <section class="bg-black py-16">
          <div class="mx-auto max-w-[1920px] px-[285px]">
            <div class="mb-8 flex items-center">
              <h2 class="text-5xl font-light text-white">{category}</h2>
              <div class="ml-0 flex-1 border-t border-white/20" />
            </div>
            <div class="grid grid-cols-4 gap-10">
              {Array.from({ length: 4 }, (_, i) => (
                <a href={`/resources/${i + 1}`} class="group w-[350px]">
                  <p class="text-base font-normal text-white/50">26/07/2026</p>
                  <div class="mt-4 h-[200px] w-[288px] overflow-hidden rounded-lg">
                    <img
                      src={`/images/backgrounds/deco-${(i % 8) + 1}.png`}
                      alt=""
                      class="h-full w-full object-cover"
                    />
                  </div>
                  <h3 class="mt-4 max-w-[288px] text-xl font-normal text-white group-hover:text-primary">
                    lorem ipsum dolor sit amet consecte adipiscing elit sed...
                  </h3>
                </a>
              ))}
            </div>
          </div>
        </section>
      ))
    } -->
  </main>
</Layout>
